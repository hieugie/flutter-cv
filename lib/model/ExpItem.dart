class ExpItem {
  int id;
  String position;
  String company;
  String fromDate;
  String toDate;
  String description;
  List<String> frameWork;
  String project;

  ExpItem(this.id, this.position, this.company, this.fromDate, this.toDate,
      this.description, this.frameWork, this.project);
}
